export class Flow {
  title: string;
  counter: number;
  url: string;

  constructor(title: string, counter: number, url: string) {
    this.title = title;
    this.counter = counter;
    this.url = url;
  }
}
